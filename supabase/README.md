# Supabase Setup

Enable Email and Google authentication.

Seed first registered user as `owner`.

Environment variables:

- NEXT_PUBLIC_SUPABASE_URL
- NEXT_PUBLIC_SUPABASE_ANON_KEY
- SUPABASE_SERVICE_ROLE_KEY
